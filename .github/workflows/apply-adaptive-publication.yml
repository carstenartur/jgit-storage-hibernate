name: Apply adaptive publication source

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled-during-payload-upload:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
